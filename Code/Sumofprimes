// Sum of prime number in a given list

def sumprimes(l):
  lt = []
  for i in l:
      count = 0
      if i > 0:
          for k in range(2,i+1):
              if i % k == 0:
                  count = count + 1
      if count == 1:
                lt.append(i)
  return sum(lt)     
